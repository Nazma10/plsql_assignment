DECLARE
  -- variables
  -- constant
  -- cursors

BEGIN
  -- Invoke APIs

  -- create containerItems
  NAZMA_CONTAINERITEMS_API.insert_data(3, 3, 'stored');

  -- update containerItems
  NAZMA_CONTAINERITEMS_API.update_containerItems(1, 1, 'empty');

  -- delete containerItems
  NAZMA_CONTAINERITEMS_API.delete_containerItems(2, 2);
END;